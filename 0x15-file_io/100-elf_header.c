#include <stdio.h>

/**
 * main - The entry point for program to get 100-elf header file
 * @argc: The number of arguments
 * @argv: An array to the pointer of arguments
 * Return: 1 on success, error if code fails
 */
int main(int argc, char *argv[])
{
	printf("argc:%d, argv:%p\n", argc, (void *)*argv);
	return (1);
}

